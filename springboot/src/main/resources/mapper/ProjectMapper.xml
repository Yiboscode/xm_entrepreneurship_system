<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ProjectMapper">

    <select id="selectAll" resultType="com.example.entity.Project">
        select project.*, teacher.name as teacherName, classify.name as classifyName from project
        left join teacher on project.teacher_id = teacher.id
        left join classify on project.classify_id = classify.id
        <where>
            <if test="id != null"> and project.id = #{id}</if>
            <if test="teacherId != null"> and project.teacher_id = #{teacherId}</if>
            <if test="teacherName != null"> and teacher.name like concat('%', #{teacherName}, '%')</if>
            <if test="classifyId != null"> and project.classify_id = #{classifyId}</if>
            <if test="classifyName != null"> and classify.name like concat('%', #{classifyName}, '%')</if>
            <if test="title != null"> and project.title like concat('%', #{title}, '%')</if>
            <if test="introduce != null"> and project.introduce like concat('%', #{introduce}, '%')</if>
            <if test="content != null"> and project.content like concat('%', #{content}, '%')</if>
            <if test="img != null"> and project.img like concat('%', #{img}, '%')</if>
            <if test="views != null"> and project.views = #{views}</if>
            <if test="status != null"> and project.status like concat('%', #{status}, '%')</if>
            <if test="reason != null"> and project.reason like concat('%', #{reason}, '%')</if>
        </where>
        order by id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Project">
        select project.*, teacher.name as teacherName, classify.name as classifyName from project
        left join teacher on project.teacher_id = teacher.id
        left join classify on project.classify_id = classify.id
        where project.id = #{id}
    </select>

    <delete id="deleteById">
        delete from `project`
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Project" useGeneratedKeys="true" keyProperty="id">
        insert into `project`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="teacherId != null">teacher_id,</if>
            <if test="classifyId != null">classify_id,</if>
            <if test="title != null">title,</if>
            <if test="introduce != null">introduce,</if>
            <if test="content != null">content,</if>
            <if test="img != null">img,</if>
            <if test="views != null">views,</if>
            <if test="status != null">status,</if>
            <if test="reason != null">reason,</if>
            <if test="time != null">time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="teacherId != null">#{teacherId},</if>
            <if test="classifyId != null">#{classifyId},</if>
            <if test="title != null">#{title},</if>
            <if test="introduce != null">#{introduce},</if>
            <if test="content != null">#{content},</if>
            <if test="img != null">#{img},</if>
            <if test="views != null">#{views},</if>
            <if test="status != null">#{status},</if>
            <if test="reason != null">#{reason},</if>
            <if test="time != null">#{time},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Project">
        update `project`
        <set>
            <if test="teacherId != null">
                teacher_id = #{teacherId},
            </if>
            <if test="classifyId != null">
                classify_id = #{classifyId},
            </if>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="introduce != null">
                introduce = #{introduce},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="img != null">
                img = #{img},
            </if>
            <if test="views != null">
                views = #{views},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="reason != null">
                reason = #{reason},
            </if>
        </set>
        where id = #{id} 
    </update>

</mapper>