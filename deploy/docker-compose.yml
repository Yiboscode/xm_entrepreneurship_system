version: '3.8'

services:
  # MySQL数据库
  mysql:
    image: mysql:8.0
    container_name: entrepreneurship_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123456  # 修改为更安全的密码
      MYSQL_DATABASE: entrepreneurship_system
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Spring Boot后端（暂时注释，等本地测试完成后启用）
  # backend:
  #   build:
  #     context: ./springboot
  #     dockerfile: Dockerfile
  #   container_name: entrepreneurship_backend
  #   restart: always
  #   ports:
  #     - "9090:9090"
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   environment:
  #     SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/entrepreneurship_system?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=GMT%2b8&allowPublicKeyRetrieval=true
  #     SPRING_DATASOURCE_USERNAME: root
  #     SPRING_DATASOURCE_PASSWORD: root123456
  #   volumes:
  #     - ./files:/app/files
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9090/"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

  # Vue前端（暂时注释，等本地测试完成后启用）
  # frontend:
  #   build:
  #     context: ./vue
  #     dockerfile: Dockerfile
  #   container_name: entrepreneurship_frontend
  #   restart: always
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - backend

volumes:
  mysql_data:
    driver: local

networks:
  default:
    name: entrepreneurship_network

